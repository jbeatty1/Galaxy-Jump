import 'phaser';

/**
 * Call this function to load every important non-tile entity into the scene.
 * 
 * @param {Phaser.Scene} scene the current scene
 */
export default function loadEntities(scene) {
    
}